name: 🧠 Daily AI Quote Update
on:
  schedule:
    - cron: "0 6 * * *" # every day at 6 AM
  workflow_dispatch:

jobs:
  update-quote:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Generate Animated Quote
        run: |
          QUOTES=(
            "Data is the new oil — pipelines are the refineries."
            "Automation is not the future; it’s the present accelerated."
            "Every dataset hides a story — engineering reveals it."
            "Think in systems, act in data."
            "AI doesn’t replace engineers — it upgrades them."
            "From chaos to insight — one pipeline at a time."
            "Smart systems aren’t built — they’re engineered."
            "Data speaks only when engineered to."
          )
          RANDOM_QUOTE=${QUOTES[$RANDOM % ${#QUOTES[@]}]}

          # Replace quote block in README
          awk -v q="$RANDOM_QUOTE" '
            BEGIN {found=0}
            /^<!--QUOTE-START-->/ {print; print "<p align=\"center\"><img src=\"https://readme-typing-svg.herokuapp.com?font=Fira+Code&size=22&duration=4500&pause=1000&color=00BFFF&center=true&vCenter=true&width=700&lines=" q "\" alt=\"AI Quote Animation\"></p>"; found=1; next}
            /^<!--QUOTE-END-->/ {found=0}
            !found
          ' README.md > temp && mv temp README.md

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "🧠 Daily Animated AI Quote Update" || echo "No changes to commit"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
